server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          #default-label: master
          default-label: CBreaker-Errors
          uri: https://github.com/repicam/microservicios-JWTgateway
          search-paths: configurations
  application:
    name: config-server